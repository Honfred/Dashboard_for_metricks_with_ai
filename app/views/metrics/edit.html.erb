<!-- filepath: /home/honfred/projects/dashboard_for_metricks_with_ai/app/views/metrics/edit.html.erb -->
<div class="container mt-4">
  <nav aria-label="breadcrumb">
    <ol class="breadcrumb">
      <li class="breadcrumb-item"><%= link_to t('breadcrumbs.metrics'), metrics_path %></li>
      <li class="breadcrumb-item"><%= link_to @metric.name, metric_path(@metric) %></li>
      <li class="breadcrumb-item active" aria-current="page"><%= t('breadcrumbs.editing') %></li>
    </ol>
  </nav>

  <div class="row justify-content-center">
    <div class="col-md-8">
      <div class="card">
        <div class="card-header bg-primary text-white">
          <h5 class="mb-0"><%= t('metrics.editing_metric', name: @metric.name) %></h5>
        </div>
        <div class="card-body">
          <%= form_with(model: @metric, local: true) do |form| %>
            <% if @metric.errors.any? %>
              <div class="alert alert-danger">
                <h4><%= t('metrics.errors.saving', count: @metric.errors.count) %></h4>
                <ul>
                  <% @metric.errors.full_messages.each do |message| %>
                    <li><%= message %></li>
                  <% end %>
                </ul>
              </div>
            <% end %>

            <div class="mb-3">
              <%= form.label :name, t('metrics.metric_name'), class: 'form-label' %>
              <%= form.text_field :name, class: 'form-control' %>
            </div>

            <div class="mb-3">
              <%= form.label :description, t('activerecord.attributes.metric.description'), class: 'form-label' %>
              <%= form.text_area :description, rows: 3, class: 'form-control' %>
            </div>

            <div class="mb-3">
              <%= form.label :metric_type, t('activerecord.attributes.metric.metric_type'), class: 'form-label' %>
              <%= form.select :metric_type, 
                  Metric.metric_types.keys.map { |key| [key.humanize, key] }, 
                  {}, 
                  class: 'form-select' %>
            </div>

            <div class="d-flex justify-content-between">
              <%= link_to t('common.cancel'), metric_path(@metric), class: 'btn btn-secondary' %>
              <%= form.submit t('common.save_changes'), class: 'btn btn-success' %>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>